# Copy this to .env and fill in your values.
# .env is gitignored — never commit it.

# IBKR account ID (optional — auto-detected on connect if blank)
IBKR_ACCOUNT=

# Override ports if needed (defaults in config.yaml are fine for most setups)
# IBKR_PAPER_PORT=4002
# IBKR_LIVE_PORT=4001
