# IBKR connectivity
ib_insync>=0.9.86

# Core (shared with research project)
pandas>=2.0
numpy>=1.24
xgboost>=2.0
scikit-learn>=1.4
joblib>=1.3
pyyaml>=6.0
pyarrow>=12.0

# Explainability
shap>=0.45

# Market data fallback (bootstrap + index regime features)
yfinance>=0.2.36

# Scheduling & utilities
schedule>=1.2
python-dotenv>=1.0
tqdm>=4.66

# Already in research project (for importing alert/feature code)
ta>=0.11
